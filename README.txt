# This script works for Google Accounts with same Business Domain only.
# The script requires an onEdit trigger to be set for a respective sheet.
# The script displays the current Active User adjecent to last edited cell.
# The edit is controlled by IF for two particular values viz "Yes" and "No", that can be modified as per the user need.